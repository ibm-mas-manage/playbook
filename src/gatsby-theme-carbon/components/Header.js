import React from 'react';
import Header from 'gatsby-theme-carbon/src/components/Header';

const CustomSwitcher = props => (
  <Header {...props}>
    <span>Playbook</span>&nbsp; for Maximo EAM Upgrade
  </Header>
);

export default CustomSwitcher;
  